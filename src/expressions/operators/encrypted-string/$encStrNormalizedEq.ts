import { StaticInput } from '../../static-input';

export interface $encStrNormalizedEq {
  $encStrNormalizedEq: (
    input: StaticInput<{
      input: unknown;
      string: string;
    }>
  ) => boolean;
}
